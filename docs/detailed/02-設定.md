# 02: 設定（`src/jevfwd/settings.py`、`src/jevfwd/judge/questions.py`、`config/`）

- 対象：`settings.py`（`Settings` `EntryRules` `Filters` `Secrets` とローダー）、`judge/questions.py`（問い版の解釈と主語埋め込み）、`config/settings.yaml` `config/entry_rules.v1.yaml` `config/filters.v1.yaml`、`docs/contracts/settings.schema.json`
- マイルストーン：M1
- 関連：CLAUDE.md ルール2・3・5、要件 F3-6・7章「保守」、基本設計 5章、ADR 002・003・016・019・020、task 014・015
- 版：2026-09-20 初版

## 1. 責務

- YAML / JSON の設定ファイルを読み、型を検証し、不変オブジェクトとして他モジュールに渡す。設定値の意味は解釈しない（周期の実行、閾値の適用は各モジュール）
- 版付きファイル（`questions.vN.json` `entry_rules.vN.yaml` `filters.vN.yaml`）は**作成後に変更しない**。変更検知は SHA-256 定数のテスト（ADR 020）
- 秘密（API キー等）は環境変数からだけ読み、ログや `repr` に出さない
- `judge/questions.py` は問い版 JSON を TypeSafe の questions 形式（v1 と同じ形）に変換する。文言は一切変えない。`{subject}` `{company}` `{code}` の置換だけを行う（ADR 002）

しないこと：`.env` の読み込み（Docker compose か `set -a; source .env` に任せる）、設定値の上書き・マージ・環境別プロファイル、TypeSafe API の HTTP 要求本体の組み立て（M4 `judge/client.py`。task 002 待ち）。

## 2. 依存

- `pyyaml`（`yaml.safe_load` のみ）、`pydantic>=2`
- `jevfwd.common`（`hashing` `errors`）
- `judge/questions.py` は `settings.py` を import しない（純関数。版の文字列と JSON 文字列だけを受け取る）

## 3. 公開インターフェース

### 3.1 `settings.py`

```python
class _Strict(BaseModel):
    model_config = ConfigDict(extra="forbid", frozen=True, str_strip_whitespace=False)

HHMM = Annotated[StrictStr, StringConstraints(pattern=r"^\d{2}:\d{2}$")]   # "18:30"。YAML では必ずクォート

class PathsCfg(_Strict):
    config_dir: str = "config"              # 版ファイルの置き場。相対パスは CWD 基準（Docker は WORKDIR=/app）
    contracts_dir: str = "docs/contracts"   # state.vN.schema.json

class StorageCfg(_Strict):
    db_path: str = "data/db/jevfwd.sqlite"
    raw_pdf_dir: str = "data/raw/pdf"
    raw_pdf_retention_days: int = 30        # ADR 018

class FetchCfg(_Strict):
    interval_sec: int = 60
    pdf_retries: int = 3
    list_lookback_days: int = 1

class BundleCfg(_Strict):
    window_sec: int = 300
    grace_sec: int = 120

class JudgeCfg(_Strict):
    api_base_url: str = "https://api.typesafe.ai"
    models: list[str] = ["jev-latest", "jev-preview"]
    timeout_sec: int = 10
    retries: int = 3
    delayed_threshold_sec: int = 300        # ADR 009
    max_state_chars: int = 40000
    question_version: str = "v2"
    state_version: str = "v2"

class MarketCfg(_Strict):
    session_open: HHMM = "09:00"
    session_close: HHMM = "15:00"
    pre_open_from: HHMM = "08:00"
    tz: str = "Asia/Tokyo"

class DigestCfg(_Strict):
    runs: list[HHMM] = ["18:30", "07:30"]   # ADR 010
    tz: str = "Asia/Tokyo"

class MarketCacheCfg(_Strict):
    enabled: bool = False                   # 段階1は無効（ADR 017）
    run_at: HHMM = "06:30"
    topix_source: str | None = None         # task 003

class MonitorCfg(_Strict):
    heartbeat_sec: int = 60
    missing_beats_alert: int = 3
    disk_alert_pct: int = 80
    daily_cost_limit_jpy: int | None = None # task 004

class Settings(_Strict):
    paths: PathsCfg = PathsCfg()
    storage: StorageCfg = StorageCfg()
    fetch: FetchCfg = FetchCfg()
    bundle: BundleCfg = BundleCfg()
    judge: JudgeCfg = JudgeCfg()
    market: MarketCfg = MarketCfg()
    digest: DigestCfg = DigestCfg()
    market_cache: MarketCacheCfg = MarketCacheCfg()
    monitor: MonitorCfg = MonitorCfg()
    entry_rules_version: str = "v1"
    filters_version: str = "v1"

    def entry_rules_path(self) -> Path: ...  # <config_dir>/entry_rules.<entry_rules_version>.yaml
    def filters_path(self) -> Path: ...
    def questions_path(self, version: str | None = None) -> Path: ...   # 既定は judge.question_version
    def state_schema_path(self, version: str | None = None) -> Path: ... # <contracts_dir>/state.<v>.schema.json

class EntryCfg(_Strict):
    direction_up_min: float
    confidence_min: float
    market_cap_max_jpy: int
    avg_turnover_20d_min_jpy: int
    pre_return_20d_vs_topix_max: float
    max_new_positions_per_day: int

class ExitCfg(_Strict):
    time_exit_days: int
    stop_loss: float
    thesis_broken_categories: list[str]     # 意味は task 015 で確定（v2 category 値か表題パターンか）
    day1_fade: Literal["record_only", "enabled"]

class SizingCfg(_Strict):
    position_pct: float
    max_positions: int

class CostCfg(_Strict):
    commission_roundtrip: float
    slippage: float

class EntryRules(_Strict):
    version: str                            # ファイル名の vN と一致必須
    entry: EntryCfg
    exit: ExitCfg
    sizing: SizingCfg
    cost: CostCfg

class Filters(_Strict):
    version: str
    exclude_name_prefixes: list[str]        # TDnet 一覧の社名接頭辞。一致で除外（ETF/ETN/REIT/インフラ）
    market_name_prefixes: list[str]         # 市場区分の接頭辞（Ｐ－ Ｓ－ Ｇ－）。社名から剥がすだけ
    exclude_code_patterns: list[str]        # 5文字コードへの正規表現。一致で除外
    exclude_titles: list[str]               # 表題の正規表現（re.search）。一致で除外（ADR 019）
    low_priority_titles: list[str]          # 一致で flags_json に low_priority:<key>（ADR 019）
    tob_titles: list[str]                   # 一致で TOB 例外（ADR 003）
    sequence_patterns: list[str]            # 連番部分。表題末尾に一致（ADR 016）

    # 純関数のヘルパ（判定の組み合わせ＝bundle_role の決定は M3 bundle/filters.py）
    def strip_market_prefix(self, company: str) -> str: ...
    def has_excluded_name_prefix(self, company: str) -> bool: ...
    def code_excluded(self, code: str) -> bool: ...
    def title_excluded(self, title: str) -> bool: ...
    def title_low_priority(self, title: str) -> bool: ...
    def title_is_tob(self, title: str) -> bool: ...
    def strip_sequence(self, title: str) -> tuple[str, str | None]: ...   # (連番を除いた表題, 一致した連番文字列 or None)

def resolve_settings_path(cli_arg: str | None, env: Mapping[str, str] = os.environ) -> Path:
    """--config > $JEVFWD_CONFIG > config/settings.yaml"""
def load_settings(path: Path) -> Settings: ...
def load_entry_rules(path: Path) -> EntryRules: ...      # version がファイル名と不一致なら ConfigError
def load_filters(path: Path) -> Filters: ...             # 各正規表現は読み込み時に re.compile して不正なら ConfigError

SECRET_NAMES = ("TYPESAFE_API_KEY", "JQUANTS_REFRESH_TOKEN", "NTFY_TOPIC")

class Secrets:
    """環境変数から読む。値は属性で取れるが repr/str には出ない。"""
    @classmethod
    def from_env(cls, env: Mapping[str, str] = os.environ) -> "Secrets": ...
    def require(self, name: str) -> str: ...            # 未設定なら ConfigError（メッセージは変数名のみ）
    def get(self, name: str) -> str | None: ...
    def values(self) -> frozenset[str]: ...             # ログのマスク用（common.logging の SecretMaskFilter が使う）
    def __repr__(self) -> str: ...                      # Secrets(TYPESAFE_API_KEY=set, JQUANTS_REFRESH_TOKEN=unset, NTFY_TOPIC=set)
```

`version_from_filename(path: Path, stem: str) -> str`：`entry_rules.v1.yaml` → `"v1"`。`stem.vN.(yaml|json)` の形でなければ `ConfigError`。`questions.py` と共用するため `common/versions.py` に置く。

### 3.2 `judge/questions.py`

```python
SUBJECT_PLACEHOLDER = "{subject}"
META_PREFIX = "_"
QTYPES = ("noul", "score", "choice")

@dataclass(frozen=True)
class Question:
    id: str
    type: str                      # noul / score / choice
    instructions: str
    criteria: dict[str, str] | list[str]   # choice/noul は dict、score は list

@dataclass(frozen=True)
class QuestionSet:
    version: str
    subject_prefix: str | None     # _subject_prefix（v1 は None）
    meta: dict[str, object]        # _ で始まるキー全部（_version, _frozen_at, _notes, _subject_prefix）
    questions: tuple[Question, ...]   # ファイル内の出現順

def parse(text: str, version: str) -> QuestionSet:
    """JSON 文字列を解釈する。DB の question_versions.json からも、ファイルからも同じ関数。
    - _ で始まるキーは meta。_version があれば version と一致必須（不一致は ConfigError）
    - 残りは問い。各問いは type / instructions / criteria を持ち、type は QTYPES、
      criteria は score なら list[str]（5要素）、noul なら keys {true,false}、choice なら dict[str,str]。違反は ConfigError
    - {subject} を含む問いがあるのに _subject_prefix が無ければ ConfigError"""

def load_file(path: Path) -> QuestionSet:
    """版はファイル名 questions.vN.json から。text = path.read_text(encoding="utf-8") を parse に渡す"""

def render(qs: QuestionSet, company: str, code: str) -> dict[str, dict]:
    """TypeSafe の questions 形式に変換する（v1 の JSON と同じ形）。
    - subject = subject_prefix.replace("{company}", company).replace("{code}", code)（subject_prefix が None なら ""）
    - instructions = instructions.replace("{subject}", subject)
    - 出力は {question_id: {"type": ..., "instructions": ..., "criteria": ...}} のみ。meta は含めない
    - criteria は変更しない。出力に "{" が残っていたら ConfigError（未知のプレースホルダの検知）
    - code は表示用4文字（common.codes.display_code の結果）を渡す。5文字のまま渡すのは呼び出し側の誤り（ValueError）"""

def render_from_db(conn, version: str, company: str, code: str) -> dict[str, dict]:
    """queries.get_question_version(conn, version) の json を parse → render。無ければ JudgeError（ADR 020）。M4 の judge が使う"""
```

## 4. データ契約

### 4.1 `config/settings.yaml`（全文。実装セッションはこのまま置く）

```yaml
# jevfwd の運用設定。周期・閾値・パスだけを置く。秘密（API キー、トークン、ntfy トピック）は環境変数。
# 全項目と既定値は docs/contracts/settings.schema.json（settings.py の Settings から生成）。
# 時刻は "HH:MM" を必ずクォートする（YAML は 18:30 を 60進数の 1110 と解釈する）。
paths:
  config_dir: config
  contracts_dir: docs/contracts
storage:
  db_path: data/db/jevfwd.sqlite
  raw_pdf_dir: data/raw/pdf
  raw_pdf_retention_days: 30
fetch:
  interval_sec: 60
  pdf_retries: 3
  list_lookback_days: 1
bundle:
  window_sec: 300
  grace_sec: 120
judge:
  api_base_url: https://api.typesafe.ai
  models: [jev-latest, jev-preview]
  timeout_sec: 10
  retries: 3
  delayed_threshold_sec: 300
  max_state_chars: 40000
  question_version: v2
  state_version: v2
market:
  session_open: "09:00"
  session_close: "15:00"
  pre_open_from: "08:00"
  tz: Asia/Tokyo
digest:
  runs: ["18:30", "07:30"]
  tz: Asia/Tokyo
market_cache:
  enabled: false
  run_at: "06:30"
  topix_source: null
monitor:
  heartbeat_sec: 60
  missing_beats_alert: 3
  disk_alert_pct: 80
  daily_cost_limit_jpy: null
entry_rules_version: v1
filters_version: v1
```

### 4.2 `config/entry_rules.v1.yaml`（全文。作成後は不変）

```yaml
# エントリー条件・出口ルール v1（要件3章・8章、基本設計5章、ADR 005・006・011・012）。
# このファイルは変更しない。変えるときは entry_rules.v2.yaml を作り、settings.yaml の entry_rules_version を上げ、件数をリセットする。
version: v1
entry:
  direction_up_min: 0.80
  confidence_min: 0.60
  market_cap_max_jpy: 100000000000
  avg_turnover_20d_min_jpy: 100000000
  pre_return_20d_vs_topix_max: 0.10
  max_new_positions_per_day: 3
exit:
  time_exit_days: 20
  stop_loss: -0.08
  thesis_broken_categories: [下方修正, 不祥事, 監理]   # 意味の確定は task 015
  day1_fade: record_only
sizing:
  position_pct: 0.05
  max_positions: 10
cost:
  commission_roundtrip: 0.001
  slippage: 0.002
```

### 4.3 `config/filters.v1.yaml`（全文。作成後は不変）

```yaml
# 除外・重要度低下・TOB 例外・連番の表題パターン v1（要件 F1-4、ADR 003・016・019、検証ログ #19〜#21）。
# 正規表現は Python re、表題には re.search、連番は表題末尾に一致。このファイルは変更しない（変更は v2 を新規作成）。
version: v1

# TDnet 一覧の社名接頭辞。一致した銘柄は判定しない（bundle_role='excluded', skip_reason='excluded_instrument'）
exclude_name_prefixes: ["Ｅ－", "Ｎ－", "Ｒ－", "Ｉ－"]       # ETF / ETN / REIT / インフラファンド

# 市場区分の接頭辞。社名を state に入れる前に剥がす。除外はしない
market_name_prefixes: ["Ｐ－", "Ｓ－", "Ｇ－"]                # プライム / スタンダード / グロース

# 5文字コードへの正規表現。一致で excluded_instrument。5文字目 4 は ETF（tests/fixtures/tdnet_list_2026-09-14_16.tsv の 13264 等）
# 整理・監理銘柄と上場後60日未満はコードでは分からないので listed_master（M6）で判定する（task 014）
exclude_code_patterns:
  - "^[0-9]{4}4$"

# 表題で除外（判定しない）。skip_reason='excluded_title'
exclude_titles:
  - "投資信託約款の変更"
  - "収益分配金"
  - "日々の開示事項"

# 表題で重要度低下（判定するが flags_json に low_priority:<key> を付け、ダイジェストから外す）
# key は上から growth_plan / cg_report
low_priority_titles:
  - "事業計画及び成長可能性に関する事項"
  - "コーポレート・?ガバナンス(に関する)?報告書"

# TOB 例外（単独 bundle。同時刻の他開示は tob_attached）
tob_titles: ["公開買付", "ＭＢＯ", "MBO", "株式交換"]

# 連番表題。表題末尾のこの部分を除いた文字列が一致する開示を1つの論理開示にする
sequence_patterns:
  - "[（(]\\s*[0-9０-９]+\\s*[／/]\\s*[0-9０-９]+\\s*[）)]\\s*$"    # （１／３）(1/3)
  - "【\\s*[0-9０-９]+\\s*】\\s*$"                                     # 【1】
  - "[（(]その\\s*[0-9０-９]+\\s*[）)]\\s*$"                            # （その１）
```

`low_priority_titles` の key（`growth_plan` `cg_report`）はリストの順序で対応させる。パターンを足すときは末尾に足し key を本節に追記する（版を上げる）。

### 4.4 `docs/contracts/settings.schema.json`

`Settings.model_json_schema()` の出力を `json.dumps(indent=2, ensure_ascii=False, sort_keys=True)` で保存したもの。手で書かない。`Settings` を変えたら再生成して commit する（テスト T02-23 が不一致を検知）。基本設計5章の `settings.schema.yaml` はこのファイルに読み替える（★）。

### 4.5 問い版 JSON の形

- ファイル名 `config/questions.vN.json`。UTF-8、BOM なし。ファイルは作成後不変（SHA-256：v1 `14a8baac5898235be756d1c4265fc39541adf13108d9bbc04a445cabf65bd2e7`、v2 `3f7b661012e0ab94c22f3012bb76a72cc2a03cd086e26b5208fc5b6455f6bcb0`。ADR 020）
- トップレベルは object。`_` で始まるキーはメタ（`_version` `_frozen_at` `_notes` `_subject_prefix`）。それ以外は問い ID → `{type, instructions, criteria}`
- `_subject_prefix` は `{company}` `{code}` を含む文字列。各問いの `instructions` は `{subject}` を含んでよい（v2 は7問すべて先頭に含む）。`{subject}` 以外の `{…}` は使わない
- `render` の出力（TypeSafe の questions 形式）は v1 のファイルと同じ形：`{"direction": {"type": "choice", "instructions": "...", "criteria": {...}}, ...}`。HTTP 要求全体（`model` `state` を含む）の形は task 002 の採取後に `07-判定.md` で決める

### 4.6 版ファイルの不変性

| ファイル | SHA-256 定数の置き場 | 値 |
|---|---|---|
| `config/questions.v1.json` | `tests/test_questions.py` | `14a8baac…bd2e7`（ADR 020） |
| `config/questions.v2.json` | 同上 | `3f7b6610…6fcb0`（ADR 020） |
| `config/entry_rules.v1.yaml` | `tests/test_settings.py` | 実装セッションで 4.2 のままファイルを作った直後に `sha256sum` で採り、定数として書く。以後は変えない |
| `config/filters.v1.yaml` | 同上 | 同上（4.3） |

`settings.yaml` は版なし（運用で変える）。SHA は取らない。

## 5. エラー時の振る舞い

| 事象 | 振る舞い |
|---|---|
| 設定ファイルが無い | `ConfigError("設定ファイルがありません: <path>")` |
| YAML 構文エラー | `ConfigError`（`yaml.YAMLError` を包む。行番号を含める） |
| 未知キー・型不一致・欠落 | `ConfigError`。pydantic の `ValidationError` を包み、メッセージにキーのパス（`market.session_open`）を含める |
| `"HH:MM"` を未クォートで書いた | pydantic に int が渡り `StrictStr` で失敗 → `ConfigError`。メッセージに「クォートが必要」を添える |
| `version` とファイル名の版が不一致 | `ConfigError` |
| 正規表現がコンパイルできない | `ConfigError`（どのキーの何番目か） |
| 秘密が未設定で `require` | `ConfigError("環境変数 TYPESAFE_API_KEY が未設定")`。値は絶対に含めない |
| `parse`：`type` 不明、`criteria` の形が違う、`_version` 不一致、`{subject}` があるのに `_subject_prefix` 無し | `ConfigError` |
| `render`：出力に `{` が残る | `ConfigError`（未知プレースホルダ） |
| `render`：`code` が4文字でない | `ValueError` |
| `render_from_db`：版が未凍結 | `JudgeError("問い版 v2 は未凍結。jevfwd freeze-questions --version v2")`（ADR 020） |

例外はすべて `common.errors` の型。pydantic / yaml の例外をそのまま外に出さない。

## 6. 設定項目

本モジュールが提供する側。読む設定は環境変数 `JEVFWD_CONFIG`（settings.yaml の場所）と `SECRET_NAMES` のみ。

## 7. 擬似コード

```python
# settings.py
def _load_yaml(path: Path) -> dict:
    if not path.exists():
        raise ConfigError(f"設定ファイルがありません: {path}")
    try:
        data = yaml.safe_load(path.read_text(encoding="utf-8"))
    except yaml.YAMLError as e:
        raise ConfigError(f"{path}: YAML が読めません: {e}") from e
    if not isinstance(data, dict):
        raise ConfigError(f"{path}: トップレベルは mapping")
    return data

def _validate(model, data, path):
    try:
        return model.model_validate(data)
    except ValidationError as e:
        locs = "; ".join(".".join(str(x) for x in err["loc"]) + ": " + err["msg"] for err in e.errors())
        hint = "（\"HH:MM\" はクォートが必要）" if any(err["type"] == "string_type" for err in e.errors()) else ""
        raise ConfigError(f"{path}: {locs}{hint}") from e

def load_settings(path):      return _validate(Settings, _load_yaml(path), path)
def load_entry_rules(path):
    rules = _validate(EntryRules, _load_yaml(path), path)
    _check_version(rules.version, version_from_filename(path, "entry_rules"), path)
    return rules
def load_filters(path):
    f = _validate(Filters, _load_yaml(path), path)
    _check_version(f.version, version_from_filename(path, "filters"), path)
    for key in ("exclude_code_patterns", "exclude_titles", "low_priority_titles", "tob_titles", "sequence_patterns"):
        for i, pat in enumerate(getattr(f, key)):
            try: re.compile(pat)
            except re.error as e: raise ConfigError(f"{path}: {key}[{i}] の正規表現が不正: {e}") from e
    return f

# questions.py
def parse(text, version):
    data = json.loads(text)                                  # JSONDecodeError → ConfigError
    meta = {k: v for k, v in data.items() if k.startswith(META_PREFIX)}
    if "_version" in meta and meta["_version"] != version:
        raise ConfigError(f"_version={meta['_version']} がファイル名の版 {version} と一致しない")
    subject_prefix = meta.get("_subject_prefix")
    qs = []
    for qid, q in data.items():
        if qid.startswith(META_PREFIX): continue
        _check_question_shape(qid, q)                        # type / instructions / criteria
        if SUBJECT_PLACEHOLDER in q["instructions"] and subject_prefix is None:
            raise ConfigError(f"{qid}: {{subject}} があるのに _subject_prefix が無い")
        qs.append(Question(qid, q["type"], q["instructions"], q["criteria"]))
    return QuestionSet(version, subject_prefix, meta, tuple(qs))

def render(qs, company, code):
    if len(code) != 4: raise ValueError("code は表示用4文字（common.codes.display_code）")
    subject = (qs.subject_prefix or "").replace("{company}", company).replace("{code}", code)
    out = {}
    for q in qs.questions:
        ins = q.instructions.replace(SUBJECT_PLACEHOLDER, subject)
        if "{" in ins: raise ConfigError(f"{q.id}: 未知のプレースホルダが残っている")
        out[q.id] = {"type": q.type, "instructions": ins, "criteria": q.criteria}
    return out
```

## 8. 受入テスト一覧

### 8.1 `tests/test_settings.py`

| ID | テスト関数 | 前提・入力 | 期待 | fixture |
|---|---|---|---|---|
| T02-01 | `test_settings_yaml_loads_with_documented_values` | リポジトリの `config/settings.yaml` | `Settings` が返り、`judge.delayed_threshold_sec==300`、`bundle.grace_sec==120`、`digest.runs==["18:30","07:30"]`、`market_cache.enabled is False`、`storage.db_path=="data/db/jevfwd.sqlite"` | `config/settings.yaml` |
| T02-02 | `test_settings_defaults_equal_repo_file` | `Settings()`（既定値のみ）と T02-01 の結果 | 等しい（既定値と運用ファイルを一致させておく） | `config/settings.yaml` |
| T02-03 | `test_settings_unknown_key_rejected_with_path` | `market: {sesion_open: "09:00"}` を含む tmp YAML | `ConfigError`、メッセージに `market.sesion_open` | — |
| T02-04 | `test_settings_missing_nested_key_uses_default` | `judge: {timeout_sec: 5}` だけの tmp YAML | `judge.retries==3`（既定）、`judge.timeout_sec==5` | — |
| T02-05 | `test_unquoted_hhmm_rejected` | `digest: {runs: [18:30, "07:30"]}`（未クォート） | `ConfigError`、メッセージに `digest.runs.0` と「クォート」 | — |
| T02-06 | `test_hhmm_pattern_enforced` | `market: {session_open: "9:00"}` | `ConfigError`（`^\d{2}:\d{2}$`） | — |
| T02-07 | `test_resolve_settings_path_precedence` | `cli_arg="/x/a.yaml"`, env `JEVFWD_CONFIG=/y/b.yaml`／cli 無し・env あり／両方無し | `/x/a.yaml`／`/y/b.yaml`／`config/settings.yaml` | — |
| T02-08 | `test_missing_settings_file_raises` | 存在しないパス | `ConfigError`、メッセージにパス | — |
| T02-09 | `test_entry_rules_v1_values` | `config/entry_rules.v1.yaml` | `entry.direction_up_min==0.80`、`entry.confidence_min==0.60`、`entry.market_cap_max_jpy==100_000_000_000`、`entry.avg_turnover_20d_min_jpy==100_000_000`、`entry.pre_return_20d_vs_topix_max==0.10`、`entry.max_new_positions_per_day==3`、`exit.time_exit_days==20`、`exit.stop_loss==-0.08`、`exit.day1_fade=="record_only"`、`sizing.position_pct==0.05`、`sizing.max_positions==10`、`cost.commission_roundtrip==0.001`、`cost.slippage==0.002` | `config/entry_rules.v1.yaml` |
| T02-10 | `test_version_must_match_filename` | 4.2 の内容を `entry_rules.v2.yaml` として tmp に置く／`version: v1` のまま | `ConfigError`；ファイル名が `rules.yaml`（版なし）も `ConfigError` | — |
| T02-11 | `test_filters_v1_regexes_compile` | `config/filters.v1.yaml` | 例外なく `Filters`。全パターンが `re.compile` できる。`exclude_name_prefixes==["Ｅ－","Ｎ－","Ｒ－","Ｉ－"]`、`tob_titles==["公開買付","ＭＢＯ","MBO","株式交換"]` | `config/filters.v1.yaml` |
| T02-12 | `test_filters_invalid_regex_rejected` | `exclude_titles: ["("]` の tmp YAML | `ConfigError`、メッセージに `exclude_titles[0]` | — |
| T02-13 | `test_title_excluded_one_etf_and_tdnet_daily` | `1369_one_etf_terms.txt` 8行目の表題「投資信託約款の変更に関するお知らせ」、`tdnet_list_2026-09-14_16.tsv` の「ＥＴＦの収益分配金見込額のお知らせ」「上場ETF（管理会社：ブラックロック・ジャパン）に関する日々の開示事項」 | 3件とも `title_excluded` True。「借入に関するお知らせ」（`4935_liberta_3days_bundle.txt`）は False | `1369_one_etf_terms.txt` `tdnet_list_2026-09-14_16.tsv` `4935_liberta_3days_bundle.txt` |
| T02-14 | `test_title_low_priority_growth_plan` | `9227_microwave_growth_plan.txt` 1行目「事業計画及び成長可能性に関する事項」、「コーポレート・ガバナンスに関する報告書」 | `title_low_priority` True。`title_excluded` False。「業績予想の修正に関するお知らせ」は False | `9227_microwave_growth_plan.txt` |
| T02-15 | `test_title_is_tob_on_three_fixtures` | `6890_ferrotec_tob_buyer_full.txt` 13行目「公開買付けの開始に関するお知らせ」、`8848_leopalace_tob_target_bundle_full.txt` 10〜11行目「公開買付けに関する賛同の意見表明及び応募推奨並びに特定子会社の異動を伴う株式譲渡に関するお知らせ」、`9435_hikari_tob_buyer_full.txt` 9〜10行目、`tdnet_list_2026-09-17.tsv` の「簡易株式交換における株式交換比率の決定に関するお知らせ」 | 4件とも `title_is_tob` True。`3560_hobonichi_fiscal_year_change.txt` 8行目「決算期（事業年度の末日）の変更及びこれに伴う定款の一部変更に関するお知らせ」は False | 左記5ファイル |
| T02-16 | `test_strip_sequence_on_chubu_three_parts` | `tdnet_list_2026-09-14_16.tsv` の中部電力3行「…調査報告書の公表（１／３）」「（２／３）」「（３／３）」、および `"調査報告書の公表(2/3)"` `"資料【1】"` `"説明資料（その２）"` | 3行とも `strip_sequence` の第1要素が同一で「…調査報告書の公表」、第2要素が `（１／３）` 等。ASCII 版・【1】・（その２）も連番として検出。「借入に関するお知らせ」は `(title, None)` | `tdnet_list_2026-09-14_16.tsv` |
| T02-17 | `test_name_prefix_and_code_patterns_on_tdnet_list` | `tdnet_list_2026-09-17.tsv` の `41690 Ｇ－エネチェンジ`、`tdnet_list_2026-09-14_16.tsv` の `89600 Ｒ－ユナイテド`、`13264` | `strip_market_prefix("Ｇ－エネチェンジ")=="エネチェンジ"`、`has_excluded_name_prefix("Ｇ－エネチェンジ")` False、`has_excluded_name_prefix("Ｒ－ユナイテド")` True、`code_excluded("13264")` True、`code_excluded("41690")` False、`code_excluded("590A0")` False | `tdnet_list_2026-09-17.tsv` `tdnet_list_2026-09-14_16.tsv` |
| T02-18 | `test_versioned_yaml_sha256_frozen` | `config/entry_rules.v1.yaml` `config/filters.v1.yaml` のバイト列 | 定数と一致（定数は実装セッションでファイル作成直後に記録） | 左記2ファイル |
| T02-19 | `test_secrets_from_env_and_masked` | `env={"TYPESAFE_API_KEY": "sk-test-XYZ"}` | `require("TYPESAFE_API_KEY")=="sk-test-XYZ"`、`repr` と `str` に `sk-test-XYZ` が含まれず `TYPESAFE_API_KEY=set` と `NTFY_TOPIC=unset` を含む、`values()=={"sk-test-XYZ"}` | — |
| T02-20 | `test_secrets_missing_raises_name_only` | 空 env で `require("NTFY_TOPIC")` | `ConfigError`、メッセージに `NTFY_TOPIC`。`get` は None | — |
| T02-21 | `test_secrets_unknown_name_rejected` | `require("FOO")` | `ValueError`（`SECRET_NAMES` 外） | — |
| T02-22 | `test_settings_paths_helpers` | 既定 `Settings` | `entry_rules_path()==Path("config/entry_rules.v1.yaml")`、`filters_path()==Path("config/filters.v1.yaml")`、`questions_path()==Path("config/questions.v2.json")`、`questions_path("v1")`、`state_schema_path()==Path("docs/contracts/state.v2.schema.json")` | — |
| T02-23 | `test_settings_schema_json_matches_model` | `docs/contracts/settings.schema.json` と `Settings.model_json_schema()` | `json.loads` した結果が等しい | `docs/contracts/settings.schema.json` |

### 8.2 `tests/test_questions.py`

| ID | テスト関数 | 前提・入力 | 期待 | fixture |
|---|---|---|---|---|
| T02-24 | `test_question_files_sha256_frozen` | `config/questions.v1.json` `config/questions.v2.json` のバイト列 | v1 `14a8baac5898235be756d1c4265fc39541adf13108d9bbc04a445cabf65bd2e7`、v2 `3f7b661012e0ab94c22f3012bb76a72cc2a03cd086e26b5208fc5b6455f6bcb0` | `config/questions.v1.json` `config/questions.v2.json` |
| T02-25 | `test_load_v2_separates_meta_and_questions` | `load_file(config/questions.v2.json)` | `version=="v2"`、`meta` のキーが `{_version,_frozen_at,_notes,_subject_prefix}`、問い ID が出現順に `direction, impact, routine, surprise, transient, conditional, category`、`subject_prefix` に `{company}` と `{code}` を含む | `config/questions.v2.json` |
| T02-26 | `test_render_v2_hobonichi` | v2 を `render(qs, "株式会社ほぼ日", "3560")` | 7問すべての `instructions` が「判定対象は、この開示の発行者である株式会社ほぼ日（証券コード3560）自身の株価。」で始まる。出力のどこにも `{` が無い。トップレベルに `_` で始まるキーが無い。各問いのキーが `{type, instructions, criteria}` のみ。`criteria` は v2 ファイルと同一オブジェクト内容 | `config/questions.v2.json` `3560_hobonichi_fiscal_year_change.txt`（社名・コードの出典） |
| T02-27 | `test_render_v2_ferrotec_uses_given_company` | `render(qs, "株式会社フェローテック", "6890")` | 全 `instructions` に「株式会社フェローテック（証券コード6890）」を含み「ほぼ日」を含まない（ADR 002 12f：主語の取り違えは safety 側に倒れるので、埋め込みが呼び出し引数だけに依存することを確認） | `config/questions.v2.json` `6890_ferrotec_tob_buyer_full.txt` |
| T02-28 | `test_render_v1_is_identity` | `load_file(config/questions.v1.json)` を `render(qs, "X", "0000")` | 出力が v1 ファイルの `json.load` 結果と等しい（`_subject_prefix` 無し、`{subject}` 無し） | `config/questions.v1.json` |
| T02-29 | `test_render_rejects_five_char_code` | `render(qs, "株式会社ほぼ日", "35600")` | `ValueError` | `config/questions.v2.json` |
| T02-30 | `test_parse_from_db_text_equals_load_file` | `text = Path(v2).read_text()`；`parse(text, "v2")` と `load_file(v2)` | 等しい。`hashlib.sha256(text.encode()).hexdigest()` がファイルバイト列の SHA と等しい（BOM 無し・末尾改行を含めた生保存が成立） | `config/questions.v2.json` |
| T02-31 | `test_parse_rejects_version_mismatch` | v2 の text を `parse(text, "v3")` | `ConfigError`、メッセージに `_version=v2` | `config/questions.v2.json` |
| T02-32 | `test_parse_rejects_bad_shape` | parametrize：`type: "text"`／`score` の `criteria` が dict／`noul` の `criteria` に `true` が無い／`{subject}` ありで `_subject_prefix` 無し／JSON 構文エラー | すべて `ConfigError` | — |
| T02-33 | `test_render_detects_unknown_placeholder` | `instructions: "{subject} {company} は…"` を持つ問いを `_subject_prefix` 付きで parse → render | `ConfigError`（`{company}` は instructions では置換しない。`_subject_prefix` の中だけ） | — |
| T02-34 | `test_render_from_db_requires_frozen_version` | `migrate` 済み DB、`question_versions` 空で `render_from_db(conn, "v2", …)`；v2 を INSERT 後に再実行 | 前者 `JudgeError`（メッセージに `freeze-questions`）、後者は T02-26 と同じ出力 | `config/questions.v2.json` |

## 9. 完了条件

- T02-01〜T02-34 が通る
- `config/settings.yaml` `config/entry_rules.v1.yaml` `config/filters.v1.yaml` が 4.1〜4.3 の全文で存在し、`docs/contracts/settings.schema.json` が生成済み
- `entry_rules.v1.yaml` `filters.v1.yaml` の SHA-256 定数が `tests/test_settings.py` に記録され、本文書 4.6 の表にも追記されている
- `.env.example` に `JQUANTS_REFRESH_TOKEN=` `NTFY_TOPIC=` の行（鍵名のみ）が追加されている

## 10. 基本設計からの差分

| ★ | 変更 | 基本設計書の修正箇所 |
|---|---|---|
| 1 | ローダーの置き場を `src/jevfwd/settings.py` に決めた（基本設計2章のツリーに無かった） | 2章ツリーに追記 |
| 2 | `settings.yaml` に `paths.*` `storage.db_path` `storage.raw_pdf_dir` `judge.api_base_url` `entry_rules_version` `filters_version` を追加 | 5章の例を差し替えず「全項目は 02-設定.md」と注記 |
| 3 | `docs/contracts/settings.schema.yaml` → `settings.schema.json`（pydantic から生成） | 5章の1語を修正 |
| 4 | `filters.v1.yaml` の構造を `exclude_name_prefixes` / `market_name_prefixes` / `exclude_code_patterns` / `exclude_titles` / `low_priority_titles` / `tob_titles` / `sequence_patterns` に確定。5章の「接頭辞 E-/N-/R-/I-」は TDnet 一覧の**社名**接頭辞（全角）だと fixture で確認 | 5章の文は残し「詳細は 02-設定.md 4.3」と注記 |
| 5 | 版付きファイルの `version:` キーとファイル名の一致を要求 | — |
| 6 | 整理・監理・上場60日未満のコード判定は `listed_master`（M6）に委ね、v1 では `exclude_code_patterns` に ETF の1本だけ | task 014 |
