# 018: 実装 M1 前半（セッション 1a：共通規約・凍結ログ）

- Status: done
- Owner: claude
- Blocks: task 019（M1 後半）、以降の全実装
- Created: 2026-09-20
- Related: `docs/実装セッション指示.md` セッション 1a（貼る文はそこ）、`docs/detailed/00-共通規約.md` `01-凍結ログ.md`、`03-CLI.md` 4章、基本設計8章 M1

## 内容
リポジトリの骨格と凍結ログを作る。作業手順と貼る指示文は `docs/実装セッション指示.md` の「共通の前置き」＋「セッション 1a」。ここには重複を書かない。

成果物の要約：
- `pyproject.toml`（03-CLI 4章）、`deploy/Dockerfile` の `dev` ターゲットと `deploy/docker-compose.yml` の `dev` サービス（本番ターゲットと supervisord は M5）
- `src/jevfwd/common/`（timeutil / codes / hashing / jsonutil / errors / logutil / versions）
- `src/jevfwd/store/`（`schema.sql` は 01 の 4.3 全文、db / rows / repo / queries / freeze / migrations）
- `tests/conftest.py`（`split_disclosures`、`load_tdnet_tsv`）、`tests/test_common.py` `tests/test_conventions.py` `tests/test_store.py`
- 各パッケージの `__init__.py`（責務の docstring のみ）

## 完了条件
- T00-01〜19、T01-01〜27 が `docker compose -f deploy/docker-compose.yml run --rm dev pytest` で通る（ホストの Python を使わない）
- 00-共通規約 15章、01-凍結ログ 9章の完了条件を満たす
- `repo.py` が INSERT 系しか公開せず、UPDATE / DELETE がトリガーで拒否される（T01-07〜10）

## メモ
- 2026-09-20：起票。task 009（kickoff 書き換え）と 017（04/05 の食い違い）の完了を受けて、実装の入口として作った
- 2026-09-20：完了。`docker compose -f deploy/docker-compose.yml run --rm dev pytest` で 90 passed（T00-01〜19、T01-01〜27 と追加1件）。作ったもの：`pyproject.toml`、`deploy/Dockerfile`（dev）、`deploy/docker-compose.yml`（dev）、`.dockerignore`、`src/jevfwd/common/`（timeutil / codes / hashing / jsonutil / errors / logutil / versions）、`src/jevfwd/store/`（schema.sql・db・rows・repo・queries・freeze・migrations/README）、各パッケージの `__init__.py`、`settings.py` と `cli.py` の docstring だけの骨格、`tests/conftest.py` `test_common.py` `test_conventions.py` `test_store.py`。
- 実測で確かめたこと：中部電力 283,113 字（CRLF のまま読むと 291,498 字）、ほぼ日 1,691 字、リベルタの「各位」は 2・270・296・337 行目、`tdnet_list_2026-09-17.tsv` の開示行はちょうど 190 件、`config/questions.v{1,2}.json` の SHA-256 は ADR 020 の値と一致。dev イメージの SQLite は 3.46.1。
- 詳細設計を直した箇所（実装で分かったこと。判断は要らない範囲）：
  - `01-凍結ログ.md` 7章の `_OPS` は関数オブジェクトではなく**名前**を持ち `getattr` で解決する（import 時に束縛すると T01-27 の monkeypatch が効かない）
  - `03-CLI.md` 4章の `pyproject.toml` に `[build-system]`・`[tool.setuptools.packages.find]`・`[tool.setuptools.package-data]`（schema.sql を同梱）を追記
  - `docs/実装セッション指示.md` セッション2の要点に「PDF は `import pymupdf`（`import fitz` は非推奨警告）」を追記
- 追加したテスト（表の外）：`test_schema_sql_matches_design`（`schema.sql` が詳細設計 4.3 とバイト一致。01-凍結ログ 9章の完了条件の自動化）
- `recursive_triggers=ON` を OFF にする変異を入れると T01-02 と T01-09 が落ちることを確認した（`INSERT OR REPLACE` の暗黙 DELETE をトリガーで止めるのに必要）
- 次（task 019）へ：`tests/test_conventions.py` の T00-19 の想定集合に `config/*.v1.yaml` 3ファイルを足すこと。`cli.py` は docstring だけなので `jevfwd --help` はまだ動かない（console script は `jevfwd.cli:main` を指している）
