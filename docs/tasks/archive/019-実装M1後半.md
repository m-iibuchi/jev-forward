# 019: 実装 M1 後半（セッション 1b：設定・CLI）

- Status: done
- Owner: claude
- Blocks: task 020（M3 前半）、段階2の `freeze-questions`（事前宣言の凍結）
- Created: 2026-09-20
- Related: `docs/実装セッション指示.md` セッション 1b、`docs/detailed/02-設定.md` `03-CLI.md`、`04-束ね.md` 4.12・4.13、ADR 020・025

## 内容
設定ローダー・問い版の読み込み・CLI を作る。手順と貼る指示文は `docs/実装セッション指示.md` の「セッション 1b」。

成果物の要約：
- `config/settings.yaml` `entry_rules.v1.yaml` `filters.v1.yaml` `trading_calendar.v1.yaml`（02 の 4.1〜4.4 全文。`filters.v1.yaml` は 04 の 4.12 を含めたものが v1）
- `src/jevfwd/settings.py`、`src/jevfwd/judge/questions.py`、`docs/contracts/settings.schema.json`（生成物）
- `src/jevfwd/cli.py`（`init-db` `freeze-questions` `freeze-state` を実装、残り9個はスタブ）
- `tests/test_settings.py` `tests/test_questions.py` `tests/test_cli.py`、`.env.example` 追記

## 完了条件
- T02-01〜41、T03-01〜22 が通り、task 018 のテストも全部通る
- 版付き YAML 3ファイルの SHA-256 が `tests/test_settings.py` と `docs/detailed/02-設定.md` 4.7 の表に記録されている（以後そのファイルを変更しない）
- `jevfwd --help` と `python -m jevfwd.cli --help` が同じ出力（コンテナ内）

## メモ
- 2026-09-20：起票。本番 DB への `freeze-questions` の実行はこのタスクに含めない（段階2開始時に人が行う。`docs/事前宣言.md`、`段階0_人手チェックリスト.md`）
- 2026-09-20：完了。`docker compose -f deploy/docker-compose.yml run --rm dev pytest` で 167 passed（T00-01〜19、T01-01〜27、T02-01〜41、T03-01〜22 と追加1件）。作ったもの：`config/settings.yaml` `entry_rules.v1.yaml` `filters.v1.yaml` `trading_calendar.v1.yaml`（詳細設計 4.1〜4.4 の全文をそのまま）、`src/jevfwd/settings.py`、`src/jevfwd/judge/questions.py`、`src/jevfwd/cli.py`、`docs/contracts/settings.schema.json`（生成物）、`tests/test_settings.py` `test_questions.py` `test_cli.py`、`.env.example` の2行。
- 版つき YAML の SHA-256（作成直後に採取。02-設定 4.7 とテスト定数に記録）：
  - `entry_rules.v1.yaml` `71a93088e9ba5bcb9398eae27991277d0b16af9ac6684c8f799ba395cb0cdf36`
  - `filters.v1.yaml` `97cde499211804b4006b25786bae963f44524dca4d71995f11f0f0145ad86420`
  - `trading_calendar.v1.yaml` `7d90c803d386ebedaf37e6a96efccdabd22c1b21f49e273e3af748ed88e25f7a`
- 起票した task：**022**（「基準日設定」の rank が T02-35 と `filters.v1.yaml` で食い違う）。rank 6（訂正・事務）を正として 02・04 の受入テストの例示を直した。`filters.v1.yaml` は変えていない。
- 実装で決めたこと（詳細設計の範囲内）：`low_priority_titles` の key は `settings.LOW_PRIORITY_KEYS`（`growth_plan` `cg_report`）と位置で対応させ、パターンが key より多ければ読み込み時に ConfigError にする。`questions.render` は `company` が空なら ValueError（ADR 002 の前提が崩れるため）。受入テストの `SystemExit(0)` は、テスト側のラッパで終了コードに変換して検査した（出力も一緒に確かめるため）。
- 変異を入れて確かめたこと：`title_is_tob` から `tob_exclude_titles` の打ち消しを外すと T02-36 が落ちる（ミンカブ「公開買付けに準ずる行為」とカカクコム「一部変更」の誤検知が復活する）。
- 次（task 020）へ：`jevfwd bundle` はまだスタブ。M3 で実装したら `cli.STUBS` から外し、T03-04 のスタブ数を 8 に直す（03-CLI 3.7）。`settings.Calendar` は実装済みなので、束ねは `load_calendar` をそのまま使える。
