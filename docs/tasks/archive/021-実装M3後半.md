# 021: 実装 M3 後半（セッション 3b：state構築と `jevfwd bundle`）

- Status: done
- Owner: claude
- Blocks: M4（判定）の着手、段階1の稼働
- Created: 2026-09-20
- Related: `docs/実装セッション指示.md` セッション 3b、`docs/detailed/05-state構築.md` `03-CLI.md` 3.8・7章、`docs/contracts/state.v2.schema.json`、ADR 026、task 017

## 内容
state 構築（builder / truncate / context）と `jevfwd bundle --once` の配線を作る。手順と貼る指示文は `docs/実装セッション指示.md` の「セッション 3b」。

成果物の要約：
- `src/jevfwd/state/{builder,truncate,context}.py`、`tests/schema_check.py`、`tests/test_state.py`
- `cli.py` の `cmd_bundle`（`state_of` を組んで `runner.run_once` に渡す。`cli` は `bundles` に INSERT しない）
- 03-CLI の `STUBS` から `bundle` を外し、T03-04 を8スタブに更新（詳細設計 → テスト → コードの順）

## 完了条件
- T05-01〜25 と T03-15（`freeze-state --version v2` が実ファイルで成功）が通り、task 018〜020 のテストも全部通る
- `jevfwd bundle --once` が fixture 由来の events から bundles を作り、`state_json` が入る
- `state` から `bundle` `judge` を import していない（T00-14）

## メモ
- 2026-09-20：起票。入れ子 state の Jev での検証は task 016（M4 の最初の `judge --once`）
- 2026-09-20：完了。`state/{context,truncate,builder}.py`、`tests/schema_check.py`、`tests/test_state.py`（T05-01〜07・T05-09〜25 の24件）、`cli.cmd_bundle`、`tests/test_cli.py` に T03-23。`docker compose -f deploy/docker-compose.yml run --rm dev pytest` で 232 passed
- T05-08（`DbMarketContext`）は M6 に送った（task 025）。段階1は `market_cache.enabled: false` で動かず、`prices.source_version` の選び方が task 003 待ちのため
- 章抜粋のキーワードが長い章題の途中に誤爆する件は task 026（章題の末尾12文字だけで探す）
- `builder._self_check` は builder が作る不変条件だけを見る。schema 全文の検証は `tests/schema_check.py` と M4 の送信前検査（05 の 5章に明記）
- M4 へ：`bundles.state_json` は生のまま入っている（キー順が送信文字列と一致）。判定側は `state_json` をそのまま送り、`state_versions.spec` と突き合わせてから投げる
