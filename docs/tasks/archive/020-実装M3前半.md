# 020: 実装 M3 前半（セッション 3a：束ね）

- Status: done
- Owner: claude
- Blocks: task 021（M3 後半）
- Created: 2026-09-20
- Related: `docs/実装セッション指示.md` セッション 3a、`docs/detailed/04-束ね.md`（task 017 追補済み）、`01-凍結ログ.md` 3.4、ADR 003・011・016・019・022〜025

## 内容
束ね（grouper / filters / runner）と営業日カレンダーを作る。手順と貼る指示文は `docs/実装セッション指示.md` の「セッション 3a」。

成果物の要約：
- `settings.Calendar` と `load_calendar`、`src/jevfwd/bundle/{grouper,filters,runner}.py`
- `store/queries.py` に M3 分の読み取り（`events_after` `event_bodies` `bundles_since` `min_uncovered_event_id` `recent_titles` `max_event_id`）
- `tests/test_bundle.py`

## 完了条件
- T04-01〜35 が通り、task 018・019 のテストも全部通る
- `run_once(conn, now, cfg, filters, calendar, state_of)` の形（task 017）。`state_of` の失敗は `state_error:*` フラグ付きの行として残る（T04-35）
- `bundle` から `market` `state` `judge` を import していない（T00-14）

## メモ
- 2026-09-20：起票。`jevfwd bundle --once` の配線は task 021
- 2026-09-20：完了。`bundle/{grouper,filters,runner}.py`、`store/queries.py` の M3 分、`tests/test_bundle.py`（T04-01〜35、41関数）。`docker compose -f deploy/docker-compose.yml run --rm dev pytest` で 208 passed（既存 167 + 41）
- 実装中に見つけた食い違いは task 023（主開示の期待値）と task 024（supersede の歯止め）に起票して詳細設計を直した。判断が要ったのは flags の引き継ぎ範囲だけで、ADR 027 にした
- `settings.yaml` に `bundle.stuck_margin_sec: 600` を足し、`docs/contracts/settings.schema.json` を再生成（版つきファイルは変更していない）
- task 021 へ：`cli.cmd_bundle` は `runner.run_once(conn, now, cfg, filters, calendar, state_of)` を呼ぶだけでよい。`state_of` は `JevfwdError` を投げてよい（行は残る）。`bundle` は M3 前半の時点でまだ `cli` のスタブのまま
