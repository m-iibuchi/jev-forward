# 028: 実装 M5（セッション 5：監視・台帳・配備）

- Status: open
- Owner: claude
- Blocks: 段階1の稼働（7日間連続稼働）
- Created: 2026-09-20
- Related: `docs/実装セッション指示.md` セッション 5、`docs/detailed/08-監視通知.md`、`03-CLI.md` 3.1・3.9、ADR 018・021・028、task 027

## 内容
監視（heartbeat・通知）、台帳（ledger のハッシュ連鎖）、原本 PDF の保持期限、本番の Docker 構成とバックアップを作る。手順と貼る指示文は `docs/実装セッション指示.md` の「セッション 5」。

成果物の要約：
- `src/jevfwd/monitor/{notify,heartbeat,ledger,prune}.py`、`settings.MonitorCfg` の追加キー
- `store/queries.py` に台帳の読み取り（`table_columns` `rows_in_id_range` `max_id` `last_ledger` `ledger_entries` `heartbeat_since` `failure_rate` 用の集計）
- `cli.py` の `heartbeat` `ledger-append` `ledger-verify` `prune-pdf`、`cmd_bundle` の周期 beat
- `deploy/Dockerfile` の `prod` ターゲット、`deploy/supervisord.conf`、`deploy/docker-compose.yml` の `app` サービス、`deploy/backup_to_nas.sh`、`docs/配備手順.md`
- `tests/test_monitor.py` `tests/test_deploy.py`

## 完了条件
- T08-01〜30 が通り、T00〜T05 も全部通る（T03-01 は 15 語、T03-04 は 7 スタブに更新）
- `docker compose -f deploy/docker-compose.yml up -d app` で `init-db` → `bundler` `heartbeat` が上がり、`heartbeat` の行が 60 秒ごとに増える（fetcher / judge_worker は `autostart=false`）
- 人手：VPS で 7 日間連続稼働（段階1の通過条件。fetcher が無い間は「監視と台帳とバックアップが 7 日間回る」ことを確認する）

## メモ
- 2026-09-20：起票。M2・M4 が保留でも着手できる（08 の 1 章）
